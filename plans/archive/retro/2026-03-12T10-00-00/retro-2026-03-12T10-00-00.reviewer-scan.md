# Reviewer Calibration Scan — 2026-03-12T10-00-00

## Source Artifacts

| Feature | Code Review | QA Report | Review Verdict | QA Verdict |
|---------|-------------|-----------|----------------|------------|
| receiving-code-review | Yes | Yes | PASS | PASS |
| phase0-reference-validator | Yes | Yes | PASS | PASS_WITH_NOTES |
| audit-remove-mcp-deps | No | Yes | — | PASS |

---

## Per-Review Analysis

### receiving-code-review

**Review verdict:** PASS. **QA verdict:** PASS. Verdicts align.

The review correctly applied "minimal depth" for a config/documentation category change. It ran a targeted checklist (9 specific grep-verifiable checks) rather than open-ended analysis. All checks matched QA findings exactly. The review added no false positives and raised no issues QA contradicted.

One minor gap: QA noted that the full test suite (criterion #18) was not executed. The reviewer did not flag this omission — but the review depth was explicitly "Minimal" by policy, so this is arguably policy-correct rather than a miss.

### phase0-reference-validator

**Review verdict:** PASS. **QA verdict:** PASS_WITH_NOTES. Verdicts diverge slightly.

The reviewer raised 4 minor suggestions. QA raised notes on the same 4 items (test numbering gap, `--undeploy --contrib` routing, no `--strict` mode test for Reference skills, model warning behavior). The reviewer and QA independently converged on identical observations — high signal alignment.

The QA verdict was PASS_WITH_NOTES rather than PASS. The reviewer issued PASS. The delta is the test suite execution: QA ran all 33 tests; the reviewer relied on static inspection. The reviewer correctly noted "All 33 tests pass" in the summary, suggesting they had access to test results, but the QA note on `--strict` mode for Reference skills was not flagged by the reviewer.

### audit-remove-mcp-deps (QA only, no code review)

**QA verdict:** PASS, with two open items.

Notable: QA explicitly called out that the validator was not run for either `audit/SKILL.md` or `sync/SKILL.md` (AC #3 for both plans). This was rated Low severity but marked as a "blocking gate per rollout plans." Integration tests were also skipped. Both open items are runtime-only checks that static review cannot substitute for — this is a process gap, not a reviewer miss.

---

## High-Value Checks (consistently valid)

- **Checklist-driven content verification** [Medium] — Both reviewers used explicit, grep-verifiable checklists (content keywords, forbidden strings, frontmatter fields) rather than impressionistic assessment. Every checklist item was confirmed correct by QA with matching evidence. This pattern produces reviews that QA can independently reproduce. Seen in: receiving-code-review, phase0-reference-validator. #reviewer #process

- **Backward-compatibility regression check** [High] — The phase0-reference-validator review explicitly verified that all 5 existing production skills still validated without changes after the new Reference path was added. QA independently confirmed this with the same 5-skill matrix. Catching regressions in adjacent production skills is high-value for infrastructure changes. Seen in: phase0-reference-validator, audit-remove-mcp-deps (QA equivalent: cross-cutting observations section). #reviewer #regression

- **Security input sanitization verification** [High] — The reviewer confirmed that `undeploy_skill()` correctly rejects path traversal (`/`, `..`) and flag injection (`-` prefix). QA independently ran manual tests against `../../../tmp` and `--flag-name`, both correctly rejected. This reviewer check directly prevented a shell injection vector from going unverified. Seen in: phase0-reference-validator. #reviewer #security

- **Plan-to-implementation fidelity check** [Medium] — Both reviews explicitly verified that implementation matched the approved plan. The receiving-code-review reviewer checked "byte-for-byte" match; the phase0-reference-validator reviewer verified all acceptance criteria from the plan. QA confirmed fidelity in both cases. This check is especially valuable for Reference archetype skills where the content IS the implementation. Seen in: receiving-code-review, phase0-reference-validator. #reviewer #fidelity

- **Acceptance criteria table as review structure** [Medium] — The phase0-reference-validator review used the plan's acceptance criteria table as the review skeleton, yielding a 14/14 pass matrix. QA used the same structure and arrived at the same results. Structuring reviews around the plan's own success criteria removes ambiguity about coverage scope. Seen in: phase0-reference-validator, audit-remove-mcp-deps (QA). #reviewer #process

---

## Overcorrections (false positives)

- **Test numbering gap flagged as a suggestion** [Low impact] — The phase0-reference-validator reviewer flagged the Test 25 → 27 numbering gap (skipping 26) as Minor Suggestion #1. QA noted the same gap but explicitly called it "cosmetically inconsistent but functionally correct." The reviewer's own recommendation was "accept the gap as cosmetic." This is a valid observation but actionability is near zero — it generated two paragraphs of explanation for a display-only artifact. Seen in: phase0-reference-validator. #reviewer #cosmetic

- **"27b" string test ID flagged as deviation** [Low impact] — Minor Suggestion #2 in phase0-reference-validator called out the `"27b"` string test number. The reviewer immediately noted it was acceptable and that the plan explicitly allowed it. This is a false positive with self-refutation inline — the finding should not have been surfaced as a separate suggestion at all. QA confirmed it was correct behavior. Seen in: phase0-reference-validator. #reviewer #cosmetic

- **`undeploy_skill()` contrib context observation** [Low impact] — Minor Suggestion #3 flagged that `--undeploy --contrib` routes to the same function as `--undeploy`. The reviewer immediately acknowledged this matches the plan's design. QA confirmed identical behavior and labeled it correct. This observation adds no actionable value and mirrors what the plan already documented. Pattern: reviewer surfaces "this works as designed" as a suggestion rather than a positive. Seen in: phase0-reference-validator. #reviewer #noise

---

## Missed by Reviewer, Caught by QA

- **Full test suite not executed (receiving-code-review, criterion #18)** [Low] — QA explicitly noted that `bash generators/test_skill_generator.sh` was not run during the QA phase (non-blocking per QA judgment). The code reviewer did not flag this gap either. For a documentation-category change, skipping the full suite is defensible, but neither the reviewer nor QA flagged it as a process note for future reference. Seen in: receiving-code-review. #reviewer #qa #process

- **Validator not run at all for audit/sync skills (audit-remove-mcp-deps)** [Medium] — With no code reviewer on this feature, QA was the only gate. QA flagged that AC #3 (running `validate_skill.py` against the modified skills) was not executed — for either skill. This was rated Low by QA but marked as a "blocking gate per rollout plans." The absence of a code review meant no second-party caught this before QA. The gap was process-level (no reviewer assigned) rather than a reviewer miss, but it illustrates the risk of skipping code review for infrastructure skill changes. Seen in: audit-remove-mcp-deps. #reviewer #qa #process

- **`--strict` mode not tested for Reference skills** [Low] — QA (phase0-reference-validator, note #3) observed that no test covers `--strict` mode with Reference skills. The code reviewer did not surface this edge case despite reviewing the same test file. Both agreed it is non-blocking given zero warnings in the current implementation, but it is a future regression risk. Seen in: phase0-reference-validator. #reviewer #qa #coverage

- **Unknown `type` value edge case not tested** [Low] — QA flagged that a Reference skill with `type: unknown` (rather than `type: reference`) emits a warning but passes — not explicitly tested. The reviewer did not flag this. Risk is low (single-user CLI), but the gap exists in test coverage for an input that touches security-adjacent validation logic. Seen in: phase0-reference-validator. #reviewer #qa #coverage

---

## Statistics

- Features analyzed: 3
- Code reviews present: 2 (receiving-code-review, phase0-reference-validator)
- QA reports present: 3 (all features)
- Reviews with PASS verdict: 2
- Reviews with REVISION_NEEDED verdict: 0
- QA verdicts: 2x PASS, 1x PASS_WITH_NOTES
- Reviewer/QA verdict alignment: 2/2 (both reviews agreed with QA direction; phase0 difference is PASS vs PASS_WITH_NOTES due to test execution scope, not substantive disagreement)
- High-value check patterns: 5
- Overcorrection patterns: 3 (all Low impact, all self-refuted inline by reviewer)
- Missed-by-reviewer patterns: 4 (all Low–Medium, none blocking)
