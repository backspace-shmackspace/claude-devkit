# Sync Review — 2026-05-09T00:00:00Z

Librarian review of CLAUDE.md and README.md against recent changes (commits 422a61d through 905b5e9).

## Verdict

UPDATES_NEEDED

---

## Required Updates

Changes that must be made for accuracy.

### CLAUDE.md

- [ ] **Update `Last Updated` date** — Line 4 reads `2026-04-08`. The quantitative scoring feature (commit 905b5e9) was merged after that date and is documented in the file but the header date was never bumped.

### README.md

- [ ] **Update `Last Updated` date** — Line 834 reads `2026-04-08` for the same reason.

- [ ] **Fix integration test count** — README line 607 says "Integration Test Coverage (18 tests)" but `scripts/test-integration.sh` header and a direct count of `run_test` calls both confirm **26 tests**. The 8 new quantitative scoring tests (4 positive, 4 negative/edge cases) were added in commit 905b5e9 but the README count was not updated.

- [ ] **Add `compute-run-score.sh` and `score-reflector.sh` to the Structure section** — The `## Structure` code block (lines 648–654) lists scripts inside `scripts/` but omits the two new scripts added in commit 905b5e9:
  ```
  ├── compute-run-score.sh   # Compute per-dimension scores from a JSONL audit log
  └── score-reflector.sh     # Deterministic score reflector (candidate learnings)
  ```

- [ ] **Add `score-dimensions.json` to the Structure section** — The `configs/` tree in `## Structure` (lines 643–646) lists `audit-event-schema.json` but omits `score-dimensions.json`, which was added in commit 905b5e9. Should appear alongside `audit-event-schema.json`:
  ```
  ├── score-dimensions.json      # Score dimension weights and logic (machine-readable)
  ```

---

## Suggested Updates

Optional improvements.

- [ ] **Fix version mismatch inside `skills/ship/SKILL.md`** — The frontmatter declares `version: 3.8.0` but three places inside the file body still reference `3.7.0`: the state file creation Python block (line 102), the Step 3a shared-deps WIP commit message (line 446), and the Step 5a pre-revision WIP commit message (line 1069). These are cosmetic (the version string is informational in commit messages and state files) but will produce misleading audit log entries where `skill_version` reads `"3.7.0"` for v3.8.0 runs. Updating these to `3.8.0` improves audit log accuracy. This is a SKILL.md issue, not a documentation issue, but it was introduced in the same 905b5e9 commit.

- [ ] **Cross-link Quantitative Scoring section in README** — The `## What's Included > Skills` section in README (line 65–66) mentions audit logging in one sentence but does not mention the new scoring feature. A brief note pointing readers to CLAUDE.md for the full scoring documentation would be helpful, since the README is often the first document new users read.

---

## Rationale

**Commit 905b5e9** (`feat(scoring): add quantitative eval/scoring system for /ship runs`) added:
- `scripts/compute-run-score.sh` — new file
- `scripts/score-reflector.sh` — new file
- `configs/score-dimensions.json` — new file
- 8 new tests in `scripts/test-integration.sh` (total now 26, was 18)
- Modified `skills/ship/SKILL.md` (version bump from 3.7.0 to 3.8.0 in frontmatter, plus scoring integration in Step 6)

The CLAUDE.md `## Quantitative Scoring` and `## Architecture / Three-Tier Structure` sections were updated in this commit and are accurate. The `## Audit Logging > Implementation` subsection in CLAUDE.md correctly describes all three new scripts. The `## Scripts` subsection of `/scripts` in CLAUDE.md is also accurate.

What was missed: README's `## Structure` code block (a lower-fidelity mirror of CLAUDE.md's three-tier diagram) was not updated to include the two new scripts or the new config file. The README's integration test count was not updated from 18 → 26. Both `Last Updated` dates were not bumped.

**Commit 422a61d** (`chore: update project learnings from /ship run`) only modified `.claude/learnings.md` — no documentation changes required.
