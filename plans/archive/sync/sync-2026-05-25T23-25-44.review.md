# Sync Review — 2026-05-25T23:25:44Z

## Verdict
UPDATES_NEEDED

## Required Updates

### CLAUDE.md
- [ ] **`score-reflector.sh` description is incomplete** — Line 245 describes the script without mentioning its scanner-aware correlation analysis capability added in commit `890cab0`. The description should note that it now correlates run scores with scanner mode cohorts in addition to computing statistics and trends.
- [ ] **`compute-run-score.sh` description is incomplete** — Line 244 does not mention that the script now outputs `scanner_mode` and `scanner_tokens` fields in the `run_score` event JSON (added in `890cab0`). These fields are consumed by `scanner-value-report.sh` and are a behavioral change to the output format.
- [ ] **`scanner_invocation` event description needs `output_token_count` field** — The Event Types table (line 201) registers the `scanner_invocation` event but does not mention that it now emits `output_token_count`. Both `skills/architect/SKILL.md` and `skills/ship/SKILL.md` explicitly capture this field in the event payload. The schema entry should reflect this.

### README.md
- [ ] **Integration test count is stale** — The "Integration Test Coverage" section on line 610 states **37 tests**. The actual count is now **42 tests** (confirmed by `test-integration.sh` running tests 1–42). The structural directory listing on line 665 also shows `37 tests` for `test-integration.sh`.
- [ ] **`scanner-value-report.sh` is absent from the `scripts/` directory tree** — The structure listing (lines 655–665) does not include `scanner-value-report.sh`, leaving the directory diagram inconsistent with the filesystem. (CLAUDE.md correctly lists it at line 75.)
- [ ] **`scanner-value-thresholds.json` is absent from the `configs/` directory tree** — The structure listing (lines 648–653) does not include `scanner-value-thresholds.json`. (CLAUDE.md correctly lists it at line 60.)
- [ ] **Integration test breakdown is incomplete** — The test breakdown list (lines 611–621) enumerates the previous 37 tests but omits the 5 new scanner value instrumentation tests (tests 38–42: `compute-run-score.sh scanner_mode extraction`, `scanner_mode absent default`, `scanner-value-report.sh empty dir`, `scanner-value-report.sh cohort table`, `scanner_invocation schema registration`).
- [ ] **`score-reflector.sh` description is incomplete** — Line 664 in the structure listing describes `score-reflector.sh` only as "Deterministic score reflector (candidate learnings)" without noting the scanner-aware analysis capability. The CLAUDE.md `/scripts` reference section (line 944) has the same gap.
- [ ] **`compute-run-score.sh` description does not mention new output fields** — Line 663 in the structure listing describes only per-dimension scores without noting `scanner_mode` and `scanner_tokens` fields now included in the output JSON.

## Suggested Updates
- [ ] **Add `scanner-value-report.sh` to the "Quantitative Scoring" or "Scripts" section of README.md** — The script has no discoverability in README.md's prose sections (only CLAUDE.md lines 291–301 cover it). A brief mention under a "Scanner Value Analysis" heading or in the scripts table would close the gap for README readers.
- [ ] **Clarify `score-reflector.sh` scope in both files** — `score-reflector.sh` operates on `/ship` runs only (same as `scanner-value-report.sh`). The current description "Reads all `run_score` events from `plans/audit-logs/`" could mislead if `/architect` ever adds `run_score` events. Adding "(`/ship` runs only)" would match the precision used in `scanner-value-report.sh`'s documentation.

## Rationale

Commit `890cab0` (scanner value instrumentation) introduced two new scripts (`scanner-value-report.sh`, `configs/scanner-value-thresholds.json`) and modified three existing scripts (`compute-run-score.sh`, `score-reflector.sh`, `test-integration.sh`) plus both skill files. CLAUDE.md was updated as part of that commit and is substantially current: it lists the new files in the directory tree, documents the scanner value analysis commands, and mentions `scanner-value-thresholds.json`. README.md was not updated in the same commit and retains pre-`890cab0` test counts and directory listings.

The CLAUDE.md gaps are narrower: the prose descriptions of `compute-run-score.sh` and `score-reflector.sh` in the "Audit Logging > Implementation" section predate the behavioral additions (new output fields, scanner correlation) and need one-line updates each.
