## Verdict
UPDATES_NEEDED

## Required Updates

### CLAUDE.md

- [ ] **Last Updated date is stale** — Header still shows `2026-05-09`. Should be updated to `2026-05-25` to reflect the codebase-scanner ship run (commit `f1843d7`).

- [ ] **`CLAUDE_DEVKIT` env var not documented as scanner dependency** — The CLAUDE.md documents `CLAUDE_DEVKIT` in the Quick Start manual installation block but does not explain that `codebase-scanner.py` depends on it at runtime (skills resolve the scanner path via `${CLAUDE_DEVKIT:-./}/scripts/codebase-scanner.py`). The "What install.sh Does" bullet list should add: "Exports `CLAUDE_DEVKIT` environment variable (also used by skills to locate `codebase-scanner.py`)." This is the env var flagged as undocumented in the task.

- [ ] **`scanner_invocation` audit event not in event-types table** — The Audit Logging section lists all event types, but `scanner_invocation` (emitted by `/architect` Step 1 and `/ship` Step 1 when the scanner runs) is absent. It should be added as a row: `scanner_invocation` / `When codebase-scanner.py runs during Step 1 context discovery`.

- [ ] **`.gitignore` recommended block missing scanner cache entry** — The "Recommended .gitignore" section does not include `~/.claude-devkit/cache/` (scanner output cache written at runtime). The scanner venv lives outside the repo at `~/.claude-devkit/scanner-venv/` so it does not need a repo-level gitignore entry, but the cache **is** written inside the user's home and should be noted. Consider adding a comment like: `# Scanner cache (outside repo, at ~/.claude-devkit/cache/) — no repo entry needed` for clarity, or omit if deemed out of scope.

### README.md

- [ ] **Skills count is wrong (12 → 13)** — The "Skills (12)" heading and the validation test coverage paragraph ("All 12 core skills validation") both miss `/fix`. The `/fix` skill exists in `skills/fix/SKILL.md`, is listed in CLAUDE.md's skill registry, and has structural tests in `test-integration.sh`. The skill count should be 13.

- [ ] **Integration test count is stale (26 → 37)** — The "Integration Test Coverage (26 tests)" block in the Testing section is outdated. `test-integration.sh` line 12 documents 37 tests (added scanner tests and fix skill structural tests since the 26-test baseline). Update to 37 and expand the bullet list to include: codebase-scanner integration tests (8 tests) and fix skill structural tests (2 tests).

- [ ] **`codebase-scanner.py` absent from scripts directory listing** — The README's "Structure" section (`scripts/` tree) lists `emit-audit-event.sh`, `audit-log-query.sh`, `compute-run-score.sh`, `score-reflector.sh` but omits `codebase-scanner.py` and `test-integration.sh`. Both should be added.

- [ ] **Unit test coverage paragraph lists 12 skills, not 13** — The test coverage section ("Test Coverage (46 tests)") lists "All 12 core skills validation" and omits `fix`. Since `fix` was added to CLAUDE.md's skill registry, the README should match. Also, CLAUDE.md now says "57 tests" for `test_skill_generator.sh`; the README says "46 tests" — one of these is wrong. The CLAUDE.md figure (57) is more recent; the README should be reconciled.

- [ ] **`What install.sh Does` section missing scanner venv step** — The "Automated Installation (Recommended)" section lists 4 bullets about what `install.sh` does (shell detection, PATH, aliases, backup) but omits the scanner venv setup step added in commit `f1843d7`. Add: "Creates optional tree-sitter venv at `~/.claude-devkit/scanner-venv/` for high-fidelity symbol extraction (falls back to regex if unavailable)."

- [ ] **`scanner-languages.json` absent from configs listing** — The "Structure" section shows `configs/skill-patterns.json`, `audit-event-schema.json`, `score-dimensions.json`, `tech-stack-definitions/`, `base-definitions/` but not `configs/scanner-languages.json` (added in commit `f1843d7`). Add it with description: language grammar config for codebase scanner.

## Suggested Updates

- **Troubleshooting section in README** — CLAUDE.md has a "Scanner venv or cache issues" troubleshooting entry (rm and reinstall steps). The README has no equivalent. Consider adding a brief entry under Troubleshooting so users who read only the README know how to recover from broken scanner state.

- **`deploy-skills` alias documented in README but not gen-agent** — The README install section uses `deploy-skills` as a convenience alias but the `gen-agent` alias is described as pointing to `generate_senior_architect.py` (single-agent), not `generate_agents.py` (unified generator). The install.sh actually sets `gen-agent` to `generate_senior_architect.py` and `gen-agents` to `generate_agents.py`. The README conflates these — add a note distinguishing `gen-agent` (single architect, legacy) from `gen-agents` (full team, preferred).

## Rationale

**Commit `f1843d7` (`feat(scripts): add codebase-scanner for deterministic symbol index`)** introduced:
- `scripts/codebase-scanner.py` — new script, documented in CLAUDE.md `/scripts` reference section but absent from README's directory tree
- `configs/scanner-languages.json` — new config, documented in CLAUDE.md `/configs` section but absent from README's directory tree
- `scripts/install.sh` changes — added scanner venv setup; CLAUDE.md "What install.sh Does" bullet list was updated but README's equivalent was not
- `scripts/test-integration.sh` changes — added 8 scanner tests + 2 fix tests, bringing total from 26 to 37; README still shows 26
- `skills/architect/SKILL.md` and `skills/ship/SKILL.md` — added `scanner_invocation` audit event emission; CLAUDE.md event-types table was not updated

**`CLAUDE_DEVKIT` env var:** exported by `install.sh` and consumed by scanner lookup in both skill files. CLAUDE.md documents the var in manual install but does not explain its runtime role as a scanner path resolver.

**README skill count (12 vs 13):** `/fix` skill was added in a prior commit and is fully documented in CLAUDE.md but the README's "Skills (12)" heading was never updated. The `test_skill_generator.sh` test count discrepancy (46 in README vs 57 in CLAUDE.md) predates this sync window but is a real inconsistency that should be corrected.
