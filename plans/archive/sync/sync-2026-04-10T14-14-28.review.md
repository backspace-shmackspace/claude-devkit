## Verdict

UPDATES_NEEDED

## Required Updates

### CLAUDE.md

- [ ] **Architecture tree: update `test-integration.sh` test count** — Line 69 shows `Integration smoke tests (8 tests)`. Commit `3b5d9ea` expanded test-integration.sh from 8 to 18 tests (10 new structural tests for threat model consumption patterns). Should read `Integration smoke tests (18 tests)`.

- [ ] **`/scripts` directory reference: update `test-integration.sh` description** — Lines 847–848 describe `test-integration.sh` as `Integration smoke tests (8 tests): emit-audit-event.sh JSONL correctness, L3 HMAC chain verification, 10+ call state persistence, and end-to-end generate/validate/deploy lifecycle`. This no longer describes the full 18-test suite. Update test count to 18 and append the new structural test coverage (threat model consumption patterns across /ship, /architect, /secure-review).

- [ ] **Security Gates section: document threat model validation gate in `/ship` Step 1** — The Security Gates subsection (lines 145–165) lists three gates (secrets scan, secure review, dependency audit) but omits the new Step 1 validation added in v3.7.0. `/ship` Step 1 now checks for a `## Security Requirements` section on security-sensitive plans, warning at L1 and blocking at L2/L3. This is a fourth gate and should be documented. Suggested entry:

  > 0. **Plan security requirements check** (Step 1): Validates that security-sensitive plans include a `## Security Requirements` section (derived from threat-model-gate output). At L1: warns if missing. At L2/L3: blocks if missing.

- [ ] **`Last Updated` header date** — Line 4 reads `**Last Updated:** 2026-03-31`. The threat-model-consumption feature shipped in commits on 2026-04-08 (per git log). Update to `2026-04-08`.

### README.md

- [ ] **Integration tests section: update test count from 8 to 18** — Lines 606–615 under "Integration Test Coverage (8 tests)" list the 8 original tests. The count and section header should be updated to 18 and the new threat model consumption structural tests added to the bulleted list.

- [ ] **`/ship` workflow steps: document Step 1 threat model validation** — Lines 254–267 enumerate the `/ship` workflow steps. Step 3 ("Read and validate plan") does not capture the new validation behavior. Step 1 in v3.7.0 now validates presence of `## Security Requirements` on security-sensitive plans. The numbered list should include this as an explicit checkpoint.

- [ ] **`/architect` description: mention Stage 2 plan content scan** — Line 52's `/architect` table entry says "Detects security-sensitive features and requires threat modeling when threat-model-gate is deployed." The `/architect` v3.3.0 upgrade (commit `3b5d9ea`) added a Stage 2 plan content scan in Step 2 that catches security-sensitive plans even when the `$ARGUMENTS` keyword heuristic misses them. The description should mention this secondary detection mechanism.

- [ ] **`/secure-review` table entry: update description** — Line 63's `/secure-review` entry reads "Deep semantic security review with data flow tracing". The skill is now v1.1.0 and, when invoked with a `THREAT MODEL CONTEXT:` block, produces a `## Threat Model Coverage` section mapping STRIDE threats to implementation status. The entry should reflect this new behavior.

- [ ] **`Last Updated` footer date** — Line 832 reads `**Last Updated:** 2026-03-31`. Update to `2026-04-08`.

## Suggested Updates

- [ ] **CLAUDE.md: add threat model gap memory note is now resolved** — The memory file (`MEMORY.md`) notes `[Threat model gate gap](project_threat_model_gap.md) — threat-model-gate output not verified downstream by /ship or /secure-review`. Commit `3b5d9ea` closes this gap. While memory management is out of scope for `/sync`, a brief note in the Roadmap section's v1.0 checklist acknowledging "threat model consumption across /architect, /ship, /secure-review" as completed could reinforce that the gap is closed.

- [ ] **CLAUDE.md: Coordinator Pattern characteristics — note security-analyst upgrade** — The Coordinator Pattern description (lines 509–541) lists generic characteristics. `/architect` v3.3.0 upgraded the security-analyst invocation from "Recommended" to "Required (when threat-model-gate deployed)". Adding a bullet noting that Coordinator skills may escalate optional sub-agents to required based on deployment state would make the pattern description more accurate.

- [ ] **README.md: Security-Sensitive Feature Development workflow — mention `## Security Requirements` section** — The "Security-Sensitive Feature Development" workflow (lines 128–147) documents the three security gates. Adding a note that plans for security-sensitive features should include a `## Security Requirements` section (generated by threat-model-gate, enforced by /ship Step 1 at L2/L3) would help users understand the end-to-end requirement.

## Rationale

**Commit `3b5d9ea`** (feat(skills): close threat model consumption gap) changed four artifacts in ways that are not fully reflected in the current documentation:

1. `/ship` bumped from v3.6.0 to v3.7.0. The version number is correct in the CLAUDE.md skill registry (line 100), but the new Step 1 behavior (plan security requirements validation that warns at L1 / blocks at L2/L3) is not described in the Security Gates section or in README.md's workflow step list.

2. `/architect` bumped from v3.2.0 to v3.3.0. The version and summary are correct in CLAUDE.md (line 99), but README.md's `/architect` table entry (line 52) only partially describes the detection mechanism — it omits the Stage 2 plan content scan added in Step 2 that provides a second pass for security-sensitive feature detection.

3. `/secure-review` bumped from v1.0.0 to v1.1.0. The version and description are correct in CLAUDE.md (line 109), but README.md (line 63) still shows the old one-line description without the `## Threat Model Coverage` output section.

4. `scripts/test-integration.sh` expanded from 8 to 18 tests. Both CLAUDE.md (line 69, lines 847–848) and README.md (lines 606–615) still reference 8 tests; neither document reflects the 10 new threat model structural tests added in `3b5d9ea`.

The `Last Updated` date is stale in both files (2026-03-31 vs the actual ship date of ~2026-04-08).
