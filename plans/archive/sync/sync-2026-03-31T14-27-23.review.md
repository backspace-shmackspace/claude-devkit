# Documentation Review: sync-2026-03-31T14-27-23

**Reviewed by:** Librarian
**Date:** 2026-03-31
**Scope:** CLAUDE.md, README.md
**Trigger:** Recent commits `2a36409`, `d3e0c5f`, `abb9cf9`, `9a5bf39`, `0204f26`

---

## Verdict

UPDATES_NEEDED

---

## Required Updates

### CLAUDE.md

- [ ] **Test suite count inconsistency (line 800 vs line 992):** The `/generators` directory reference at line 800 says `test_skill_generator.sh — Test suite (46 tests)` but the Validation section at line 992 says `Coverage (45 tests):`. These two references are inconsistent with each other. The README expected output block (lines 590-591) shows `Total: 46 / Pass: 46`, matching line 800. Line 992 should be updated from `(45 tests):` to `(46 tests):` to be consistent.

- [ ] **Roadmap test suite count (line 1152):** `Test suite (45 tests, all 12 core + 3 contrib skills validated)` should be updated to `(46 tests, ...)` to match the actual test count reported by the test runner output.

- [ ] **Integration test script not mentioned:** `scripts/test-integration.sh` exists and runs 8 integration smoke tests covering `emit-audit-event.sh` correctness, L3 HMAC chain verification, and 10+ call state persistence. It is not mentioned anywhere in CLAUDE.md. The `/scripts` directory reference section (lines 834-863) lists scripts but omits `test-integration.sh`. It should be added with a brief description.

- [ ] **Security gate step reference for dependency audit (line 193) uses outdated step number:** The Maturity-Aware Retention table notes "Committed to git via `git add --force` in Step 6". However, in the actual `ship/SKILL.md`, the dependency audit and commit finalization both occur in Step 6, while the old step numbering convention from prior versions (pre-worktree isolation) mapped this to a different step. The text is functionally correct — Step 6 is the commit gate — so this is accurate for the current SKILL.md. No change needed here.

### README.md

- [ ] **Test suite count inconsistency (line 573):** `Test Coverage (45 tests):` should read `Test Coverage (46 tests):` to match the expected output block immediately following it (lines 590-591: `Total: 46 / Pass: 46`). This is a self-contradicting section within README.md itself.

- [ ] **Integration test script not mentioned in Testing section (lines 564-595):** The Testing section describes only `test_skill_generator.sh`. The integration smoke test script `scripts/test-integration.sh` (which tests the `emit-audit-event.sh` infrastructure that was added in commit `0204f26` and refined in `2a36409`) is not mentioned. Since the README testing section is user-facing, it should note that integration tests exist and how to run them.

---

## Suggested Updates

- [ ] **CLAUDE.md Last Updated date (line 4):** Still reads `2026-03-27`. The file was last substantively updated by commit `abb9cf9` on 2026-03-27, and the audit logging section reflects the state as of that commit accurately. However, with the completion of audit logging instrumentation in `2a36409` (2026-03-31), a date bump to `2026-03-31` would be accurate.

- [ ] **README.md Last Updated date (line 813):** Same situation — reads `2026-03-27`. Consider bumping to `2026-03-31`.

- [ ] **CLAUDE.md /ship skill registry description (line 99):** The purpose field is accurate but dense. The SKILL.md now has 8 top-level steps (Step 0 through Step 7) with sub-steps 3a-3f and 4a-4d. The registry entry's "8 Steps" count matches the top-level step count (Steps 0-7) and is correct as-is.

- [ ] **CLAUDE.md emit-audit-event.sh description (line 222):** The description says "Reads state from a per-run state file (shell variables don't persist across Bash tool calls)." This is accurate. The description could optionally note the symlink attack prevention added in the implementation, but this level of detail is not critical for the overview.

---

## Rationale

### Test count inconsistency (45 vs 46)

Both CLAUDE.md and README.md contain a self-contradicting pair of references in their test documentation:

- **CLAUDE.md line 800:** "Test suite (46 tests)" — correct
- **CLAUDE.md line 992:** "Coverage (45 tests):" — stale
- **CLAUDE.md line 1152:** "Test suite (45 tests...)" — stale
- **README.md lines 573 and 590-591:** "Test Coverage (45 tests)" followed immediately by expected output showing "Total: 46" — self-contradicting

The `test_skill_generator.sh` script produces `Total: 46 / Pass: 46`, confirming 46 is correct. The `(45 tests)` text predates the addition of a test (likely the journal-review contrib skill validation test added before the current audit logging work). These stale counts were not caught in commit `abb9cf9` which last updated both files.

### Missing integration test script references

`scripts/test-integration.sh` was added or substantially expanded in commit `2a36409` ("complete audit logging instrumentation") and its predecessor `0204f26`. The header comment confirms 8 tests covering: coordinator lifecycle, validate-all, pipeline lifecycle, unit meta-test, emit-audit-event JSONL correctness, L3 HMAC chain, 10+ call state persistence, and cleanup.

Neither CLAUDE.md nor README.md mentions this script. Given that `emit-audit-event.sh` is now a core piece of the audit logging infrastructure (invoked by /ship, /architect, /audit on every run), the integration tests that verify its behavior should be discoverable in the documentation.

### Content accuracy of emit-audit-event.sh and ship SKILL.md descriptions

The CLAUDE.md Audit Logging section accurately describes:
- `emit-audit-event.sh` behavior (state file reads, python3 JSON escaping, exit-0 on errors)
- Event types emitted
- Log file locations
- Maturity-aware retention behavior
- Query utility usage

The ship SKILL.md now instruments all steps (0 through 7) including the substep retrospective markers for Steps 4a-4d. The CLAUDE.md description of `/ship` in the skill registry (line 99) is accurate and does not need updating — it describes the high-level workflow correctly. The security gate step references (`Step 4d verification` and `Step 6 commit gate`) in the Security Gates section (lines 148-152) match the actual SKILL.md step numbering precisely.
