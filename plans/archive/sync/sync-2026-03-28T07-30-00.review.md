---
reviewer: Librarian (sync review)
date: 2026-03-28T07:30:00Z
scope: recent (commit 0204f26 — audit logging infrastructure)
---

## Verdict

UPDATES_NEEDED

---

## Required Updates

### CLAUDE.md

- [ ] **Architecture tree: add `plans/audit-logs/` directory.** The Three-Tier Structure tree at the top of the Architecture section (`configs/`, `scripts/`, etc.) does not show the new `plans/audit-logs/` directory. This is now a first-class artifact directory, distinct from `plans/archive/`. Add it under a `plans/` entry or note it alongside the scripts block.

- [ ] **Architecture tree: add new scripts.** `emit-audit-event.sh` and `audit-log-query.sh` are present in `/scripts` (confirmed in the `/scripts` directory reference section at line 836) but are absent from the top-level directory tree at lines 60–63. The tree currently only lists `deploy.sh`, `install.sh`, `uninstall.sh`, `validate-all.sh`.

- [ ] **`audit-log-query.sh` description at line 837: missing two commands.** The `/scripts` directory reference describes the query utility as supporting `(summary, timeline, security, verify-chain, recent)`. The actual script also supports `verdicts` and `files`. Both are documented in the script's `--help` output and are real, callable commands. Update the parenthetical to include them.

- [ ] **Query Utility example block: `audit-log-query.sh` dependency on `jq` is undocumented.** The script hard-requires `jq` and exits 1 with an install hint if it is absent. The Audit Logging section documents prerequisites for `verify-chain` (implicitly openssl via the HMAC chain), but does not note that `jq` is required for all commands. Add a "Requirements: jq" note alongside the query utility example block.

- [ ] **`.gitignore` recommended snippet is stale.** The Version Control → Recommended `.gitignore` section (lines 1087–1111) does not include the audit-related gitignore patterns added in commit 0204f26: `plans/audit-logs/*.jsonl`, `.ship-audit-state-*`, `.architect-audit-state-*`, `.audit-audit-state-*`, `.ship-audit-key-*`. Users copying this snippet to new projects will miss L1 ephemeral-log ignoring and will inadvertently track L3 key files. Add a `# Audit logs (L1 ephemeral)` block matching the actual `.gitignore`.

- [ ] **Roadmap v1.0: add audit logging as a completed item.** The v1.0 checklist (lines 1127–1137) does not mention structured JSONL audit logging, which shipped in 0204f26 and is a substantive capability. Add `- [x] Structured JSONL audit logging (ship, architect, audit) with maturity-aware retention and query utility`.

### README.md

- [ ] **Skills table count: listed as 13 but there are 12 core skills.** Line 46 reads `### Skills (13)` but the table below it lists 12 skills (matching the CLAUDE.md Skill Registry). This was incorrect before the audit-logging commit and remains incorrect. Fix to `12`.

- [ ] **Structure tree: missing `scripts/emit-audit-event.sh` and `scripts/audit-log-query.sh`.** The Structure section (lines 624–626) shows the scripts directory with only `deploy.sh`, `install.sh`, `uninstall.sh`, `validate-all.sh`. The two new audit scripts are absent. Add them.

- [ ] **Structure tree: missing `configs/audit-event-schema.json`.** The configs directory entry in the structure tree does not list `audit-event-schema.json`, which is now a committed, versioned artifact (not ephemeral). Add it under configs.

- [ ] **No mention of audit logging capability.** The README is the first-contact document. The Audit Logging section exists only in CLAUDE.md. README should include at least a brief callout — one sentence or a link — pointing readers to the audit logging feature, particularly since it affects `/ship`, `/architect`, and `/audit` behavior in non-trivial ways at L2/L3 (logs committed to git).

- [ ] **`.gitignore` recommended snippet is stale (same gap as CLAUDE.md).** Lines 722–744 reproduce the pre-audit gitignore. Missing the same five patterns described above for CLAUDE.md.

---

## Suggested Updates

- [ ] **CLAUDE.md — Audit Logging section: note the `jq` requirement in the Query Utility description** (not just in the example block header). Users running `audit-log-query.sh summary` without `jq` will get a confusing error; a proactive "Requires: jq, openssl (for verify-chain)" line at the top of the section would prevent that.

- [ ] **CLAUDE.md — Development Rules: mention audit log state-file lifecycle.** The Development Rules / For Skills section describes patterns to follow. A brief bullet noting that skills using `emit-audit-event.sh` must create a state file at Step 0 and delete it at run end (or on failure) would help future skill authors adopting audit logging.

- [ ] **CLAUDE.md — version header: bump Last Updated.** Line 4 reads `2026-03-27`. The audit logging commit landed 2026-03-27 but this sync review is dated 2026-03-28. Update to `2026-03-28` after applying changes.

- [ ] **README.md — Last Updated.** Line 795 reads `2026-03-27`. Same date-bump applies.

---

## Rationale

All required updates stem from commit `0204f26` (feat: add structured JSONL audit logging to /ship, /architect, and /audit), which:

1. Added two new scripts (`scripts/emit-audit-event.sh`, `scripts/audit-log-query.sh`) that are not reflected in the directory trees in either CLAUDE.md or README.md.
2. Added `configs/audit-event-schema.json` which is missing from the README structure tree.
3. Added `plans/audit-logs/` as a new first-class artifact directory not shown in the CLAUDE.md architecture tree.
4. Modified `.gitignore` with five new patterns not reflected in the "Recommended .gitignore" snippet in either doc.
5. Added a `verdicts` and `files` sub-command to `audit-log-query.sh` that the CLAUDE.md query utility description omits (line 837 lists only five of seven commands).

The README skill count discrepancy (`### Skills (13)` vs. 12 actual) is a pre-existing bug that should be corrected in the same pass.

The Roadmap omission is minor but meaningful for project history — audit logging is the largest capability addition to v1.0 since the initial security maturity levels, and deserves a checkmark.
