# Test outputs
test-output/
*.test.md
.test/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
.venv
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject
.settings/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/
*.tmp

# Claude-specific
.claude/cache/
.claude/temp/

# Generated files (keep source templates)
# Note: skills/ and templates/ are source of truth, keep in git

# Machine-specific Claude Code permissions (not for version control)
.claude/settings.local.json

# Audit logs (L1 advisory logs are ephemeral -- L2/L3 use git add --force to commit)
plans/audit-logs/*.jsonl

# Audit run state files (ephemeral -- created at Step 0, deleted at Step 6)
.ship-audit-state-*
.architect-audit-state-*
.audit-audit-state-*

# L3 HMAC key files (committed via git add --force at L3 -- gitignored to keep L1/L2 clean)
.ship-audit-key-*
